




export const getAudio = async (text: string, model: any, cb?: Function) => {
    const o = 80
    let playing = true
    let timer = null


    const start = () => {}
    const stop  = () => {}

    return {
        start,
        stop
    }
}